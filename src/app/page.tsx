
import AuthForm from "@/components/auth-form";

export default function AuthenticationPage() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center p-4 auth-gradient">
      <AuthForm />
    </main>
  );
}
